#include<stdio.h>
int main(){
    int a = 10;
    // The entry into the loop is controlled by the condition
    // Entry Controlled Loop
    while (a < 50){
        // statements
        printf("%d ", a);
        a++; 
    }
    return 0;
}